import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Transform Your Body & Life - Fitness & Nutrition Coaching",
  description:
    "Get personalized fitness and nutrition coaching that fits your lifestyle. Science-based approach, sustainable results.",
  keywords: "fitness coaching, nutrition coaching, personal trainer, weight loss, muscle building",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
